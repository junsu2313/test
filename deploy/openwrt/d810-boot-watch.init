#!/bin/sh /etc/rc.common

START=96
STOP=15
USE_PROCD=1

PROG=/usr/bin/d810-boot-watch

start_service() {
    procd_open_instance
    procd_set_param command "$PROG"
    procd_set_param respawn 3600 5 -1
    procd_close_instance
}
