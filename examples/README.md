# Examples

This directory is reserved for safe configuration examples and hardware-independent sample inputs.

It must not contain camera originals, router credentials, access tokens, or private network exports.
